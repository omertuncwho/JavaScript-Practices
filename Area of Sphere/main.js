function area(r){
let calculate = 4*Math.PI*r**2;
console.log(`Area of the Sphere is: ${calculate}`);
}
area(12);

function volume(r){
    let calculate = (4*Math.PI*r**3)/3;
console.log(`Volume of the Sphere is ${calculate}`);
}
volume(12);